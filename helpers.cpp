// not sure why but this isn't working here so I moved it to the main .ino
//void arrayShift(double* arr, double val, int len) {
//  memmove(&arr[1], &arr[0], (len-1)*sizeof(double));
//  arr[0] = val;
//}
